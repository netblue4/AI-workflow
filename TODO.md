# AI Workflow — ToDo List

## Open

- [ ] **OWASP controls — replace custom summaries with verifiable OWASP-sourced controls** *(do this before resolving structural tension — see item below)* — The 25 OWASP controls currently in `tbl_Risk_Controls.json` are custom-written summaries shaped to fit the framework schema. They are directionally correct but are not formally traceable to the OWASP LLM Top 10 documentation. Before the structural tension between OWASP controls and Harmonised Standard controls can be resolved, this list must be replaced with a complete, verifiable control set sourced directly from the official OWASP LLM Top 10 mitigations (current version: 2025). Each control should carry a traceable OWASP reference (e.g. LLM01:2025 mitigation 1) so the source can be verified. Research task: obtain the official OWASP LLM Top 10 mitigation list, compare against current controls, identify gaps or inaccuracies, and propose replacements.

- [ ] **Provider vs Deployer — deployment model routing and risk filtering** *(depends on OWASP controls item above being resolved first)* — The workflow should support two deployment models: (1) the organisation builds a custom AI system using RAG — making them the **Provider** under the EU AI Act (Art. 9–17 obligations); (2) the organisation subscribes to a third-party AI service such as Claude for Business or Microsoft Copilot — making them the **Deployer** (Art. 26 obligations). These two roles carry fundamentally different compliance obligations and control responsibilities. Note to self: consider adding a dedicated wizard in Step 3 to determine provider vs deployer status, since the AI Act has specific criteria for this classification (including the "substantial modification" trigger in Art. 25 that can convert a deployer into a provider). The deployment model answer should cascade into Step 8 to filter risks and controls: Provider sees full risk register + OWASP implementation controls + task codes; Deployer sees deployer-level HS controls only + a new "Provider Due Diligence" risk family (vendor assessment, contractual SLAs, incident reporting obligations) replacing the OWASP implementation controls. Also requires adding an `applicable_deployment_model` field (`"provider"` / `"deployer"` / `"both"`) to `tbl_Risks.json`.

- [ ] **ART-011 (Art.43 Conformity Assessment) — consider replacing standard source** — HS-081 to HS-085 currently use `[18286.16–20]` (prEN 18286 QMS sub-references) to describe conformity assessment requirements. The dedicated official standard is **ISO/IEC 42006** (AI Management System Auditors, currently at DIS stage). Review whether to rebase ART-011 entries onto 42006 clauses once that standard is published, or keep the current 18286-derived content as an interim measure.

- [ ] **Step 7 — DPIA: explore reshaping the DPIA to match the risk assessment layout** — ask Claude whether the DPIA wizard can be restyled or restructured to look and feel like the Step 8 risk assessments, for a more consistent user experience across the workflow.

- [ ] **Step 3 → Step 8 — Use classification answers to pre-filter risks** — Step 3 already captures whether the AI use case is classified as high-risk AND whether the organisation is deploying a high-risk AI system. Ask Claude whether these answers can be used to automatically filter or suppress irrelevant risks in the Step 8 risk assessment, reducing noise for lower-risk use cases.

- [ ] **Step 9 — DPIA-identified controls: should they surface in control identification?** — Ask Claude: when the Step 7 DPIA identifies controls that need to be added (e.g. data minimisation, access restrictions), should those controls also be displayed or pre-populated in Step 9 Control Identification and Disclosure Design? Clarify the intended data flow between DPIA outputs and the Step 9 control register.

---

## Completed

- [x] **Review controls linked to OWASP risks** — All 25 OWASP controls now carry `standard_ref` values mapped to the appropriate prEN harmonised standard clauses (prEN 18282 Cybersecurity, prEN 18229-1/2 Trustworthiness, prEN 18283 Bias, prEN 18284 Data Governance, prEN ISO/IEC 24970 Logging, ISO/IEC 42001 AI Management). Three stale `[24368.x]` references on RISK-019 controls also corrected to `[12792.x]`. Note: controls are currently custom summaries — formal sourcing is tracked as an open item above.

- [x] **Check AI Act ↔ Harmonised Standards mapping** — Three issues found and resolved: (1) HS-004/005 `[18229-1.4–5]` logging standards moved from ART-001 (Art.13) → ART-005 (Art.12); (2) ART-012 (Art.50) standard refs corrected from `[24368.x]` (ISO/IEC TR 24368 — ethics/societal concerns) → `[12792.x]` (prEN ISO/IEC 12792 — Transparency Taxonomy, the correct Art.50 standard); (3) ART-011 (Art.43) noted as using prEN 18286 sub-refs instead of ISO/IEC 42006 — flagged as open item above pending 42006 publication.
